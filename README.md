# GitHub---GKE-Jenkins-via-Helm---Terraform-creates-infra---Jenkins-Kaniko-build-Artifact-Registry
GitHub -> GKE (Jenkins via Helm) -> Terraform (creates infra) -> Jenkins (Kaniko build) -> Artifact Registry -> GKE (Go App) -> LoadBalancer + API Gateway
